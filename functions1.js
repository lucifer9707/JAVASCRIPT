// const age = (age) => {
//     return age >= 18 ? "eligible for vote." : "not eligible for vote.";
// } 

function validvoter(age){
    if(age>=18){
        return "Invalid Voter";
    }
    else{
        return "Invalid Voter";
    }
}

const voter1 = validvoter(20);
console.log(voter1);